# 时序感知知识库构建文档

现代RAG（Retrieval\-Augmented Generation）和智能体架构在处理问答任务时，依赖于能够随时间动态更新的知识库，这类知识库通常包含财务报告、技术文档等持续变化的信息。为确保推理和规划过程的逻辑性与准确性，需要建立相应的时序管理机制。

# 一\.动态知识库将最终的动态知识图存储在可扩展的云数据库中

### **1\.基于百分位数的语义分块技术（其他策略如父子段分块，大模型语义分块等等）**

1\.按句子分向量处理，然后设置百分位数阈值处理合并成chunk

2\.添加元数据包含时间戳等等信息

### **2\.基于智能体的****原子事实****提取（把文档或者数据全部抽象成新的类型）**

将chunk通过llm整合成一些最小单元作为事实（减少幻觉方便迭代）比如三元组主谓宾等等，保证时间节点连贯性。

Pydantic模型：

https://github\.com/pydantic/pydantic\.git

每个类别捕获不同类型的时间引用：ATEMPORAL表示普遍正确且不随时间变化的语句（如"水在100摄氏度沸腾"）；STATIC表示在特定时间点变为真实且此后保持不变的语句（如"John于2020年6月1日被聘为经理"）；DYNAMIC表示可能随时间变化且需要时间上下文准确解释的语句（如"John是团队经理"）。

```Python
from enum import Enum
  
 class TemporalType(str, Enum):  
     ATEMPORAL = "ATEMPORAL"  # Facts that are always true (e.g., "Earth is a planet")  
     STATIC = "STATIC"        # Facts about a single point in time (e.g., "Product X launched on Jan 1st")  
     DYNAMIC = "DYNAMIC"      # Facts describing an ongoing state (e.g., "Lisa Su is the CEO")
```

StatementType枚举定义了语句的性质类型：FACT表示在表述时为真实但可能后续变化的语句（如"公司上季度收入500万美元"）；OPINION表示个人信念或感受，仅在表述时为真（如"我认为该产品会表现良好"）；PREDICTION表示对未来的预测，从当前到预测时间结束都具有有效性（如"明年销售额将增长"）。

通过定义这些固定类别，确保智能体在信息分类时的一致性。

### **3\.实体解析与去重机制**

处理实体冗余问题，比如同一个名词在不同的文档中是不同的题型，有的是全名有的是缩写\-\-必须解析去重进一步减少幻觉增加命中（pydantic）

批处理entity



完成了数据库的构建（加上传统步骤分块向量化存储）特殊操作为了应对和收集时序信息

# 二\.时序失效与知识图重构建

随着时间推移，某些事实可能变得过时或被新信息所取代。时序失效模块负责识别这些矛盾并维护知识库的一致性。

### **冲突检测机制实现设计冲突检测算法（需要设计或者找开源的，还没做调研）**

### NetworkX构建动态知识图

知识图支持动态更新

### 多智能体系统验证

# **三\.向量数据库选型**：

推荐使用支持原地更新或高效Upsert的数据库，如 [**Milvus**](https://milvus.io/)、[**Pinecone**](https://www.pinecone.io/)、[**Qdrant**](https://qdrant.tech/) 或 [**Elasticsearch**](https://www.elastic.co/)。

以上参考https://cloud\.tencent\.com/developer/article/2557235文档设计时序感知知识库

# 四\.管理知识库更新与刷新周期

### 1\.定义更新策略：完全更新与增量更新

咱们肯定选择增量更新依赖数据库的数据库的不需要完全索引更新

#### 完全重新索引

在完全重新索引策略中，整个知识库会定期被重新处理和重新索引。这包括：

1. 摄取所有源文档。

2. 对其进行分块和预处理。

3. 为所有分块生成新的嵌入 \(embedding\)。

4. 从头构建一个新的向量 \(vector\)索引（以及任何关联的元数据存储）。

5. 用新索引替换旧索引。

完全重索引能确保一致性但是资源耗费太多

#### 增量更新（如果拉取数据库更新不知道会不会更简洁\-拉取数据库更新大概率需要全量更新或者遍历比对更新值）

增量更新专注于只处理变更：新文档、已更新文档和已删除文档。这种方法需要更复杂的逻辑：

类似于端到端文件传输需要一个标识来标识这个文件Hash或者时间戳等等

1. **变更检测****：** 识别源数据中发生了什么变化。这可以通过以下方式实现：

    - **时间戳：** 跟踪文件或数据库记录上的 `last_modified` 日期。

    - **校验和/哈希：** 比较文档内容的哈希值以检测修改。

    - **版本控制系统：** 如果数据存储在Git等系统中，可以使用提交历史。

    - **事件溯源/消息队列：** 消费表示数据变更的事件。

2. **处理新文档：** 新文档被分块、嵌入，其向量被添加到现有索引中。

3. **处理已更新文档：**

    - 识别与已更新文档对应的旧分块/向量。

    - 从索引中删除这些旧向量。

    - 重新处理已更新文档，生成新的嵌入，并将新向量添加到索引中。

    - 管理此过程可能很复杂，通常需要在源文档ID与其在数据库中的向量ID之间建立映射。

4. **处理已删除文档：**

    - 识别与已删除文档对应的向量。

    - 从索引中删除这些向量。许多向量数据库都有按ID删除向量的特定API。有些数据库可能会先执行“软删除”，然后定期进行压缩以回收空间并提升性能。

### 2\.设计刷新周期：（暂时不重要，不急着确定）

但是就是个trade\-off权衡：数据时效性，运营成本和更新系统负载的收益trade\-off

#### 构建自动化更新管道：

1. 构建新的agent

2. 设置触发器或者更新计划（时间设置）去到知识数据来源pull新数据

3. 检测所有变更的文件或者说事实（前文提到的最小事实块）我们大概率数据来源不是文档而是数据库，那么检测的变更对象就是最小更改单元所谓的事实而不是文档，前文的构建时序知识库需要用到。

4. 更新的数据进行embedding然后重构成最小事实然后替换。

5. 添加元数据等等重复建库过程即可（采用增量更新）

#### 总结流程

- **源监控/触发：** 检测变更或按计划运行。

- **数据摄取：** 从源中获取新文档或更新的文档。

- **预处理和分块：** 应用与初始知识库创建时相同的清洗、转换和分块逻辑，以确保一致性。

- **嵌入 \(embedding\)生成：** 计算新分块或修改分块的嵌入。如果使用自托管模型或为了管理API成本，批量处理在这里对提高效率很重要。

- **向量 \(vector\)数据库更新：** 插入新向量，更新现有向量（通常是删除后添加的操作），并删除已删除文档的向量。关联的元数据也必须更新。

- **验证和质量检查：** （详情如下）。

- **日志记录和告警**

### 3\.版本控制和回滚：

#### 1\.版本控制：

使用索引快照\+别名不停更新指向新版本

#### 2\.回滚过程：

别名回滚类似于指针比较好实现

### 更新质量检测：

#### 每次给出更新日志

包括更新条目数量

#### 预定义召回率测试：

准备测试查询语句，计算召回率



